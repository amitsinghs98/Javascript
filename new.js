const button = document.getElementById('my-button');
const input = document.getElementById('input')


async function getData(){
    fetch(``)// insert dynamically with the template literals
}

button.addEventListener('click', ()=> {

})